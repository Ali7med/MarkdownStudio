<UserControl x:Class="MarkdownStudio.Controls.ProjectSearchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Visibility="Collapsed" Background="#55000000">
    <Border Width="680" MaxHeight="520" VerticalAlignment="Top" HorizontalAlignment="Center"
            Margin="0,70,0,0"
            Background="{DynamicResource ApplicationBackgroundBrush}"
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="1" CornerRadius="10" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <StackPanel Orientation="Horizontal">
                    <ui:TextBox x:Name="QueryBox" Width="470" Icon="{ui:SymbolIcon Search24}"
                                PlaceholderText="بحث في كل ملفات المشروع…"
                                PreviewKeyDown="OnQueryKeyDown" />
                    <ToggleButton x:Name="CaseToggle" Content="Aa" ToolTip="مطابقة الحالة"
                                  Padding="8,4" Margin="6,0,2,0" />
                    <ToggleButton x:Name="RegexToggle" Content=".*" ToolTip="Regex"
                                  Padding="8,4" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <ui:TextBox x:Name="ReplaceBox" Width="470" Icon="{ui:SymbolIcon ArrowRepeatAll24}"
                                PlaceholderText="استبدال بـ… (اختياري)" />
                    <ui:Button Content="استبدال الكل" Appearance="Secondary"
                               Margin="6,0,0,0" Padding="8,4" Click="OnReplaceAll" />
                </StackPanel>
            </StackPanel>

            <TextBlock x:Name="StatusText" Grid.Row="1" Margin="2,6,0,4"
                       FontSize="12" Opacity="0.6" Text="اكتب واضغط Enter" />

            <ListBox x:Name="ResultList" Grid.Row="2" BorderThickness="0"
                     Background="Transparent" MaxHeight="420"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="OnResultActivate" KeyDown="OnResultKeyDown">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4,3">
                            <TextBlock Text="{Binding Preview}" FontSize="13"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock FontSize="11" Opacity="0.6">
                                <Run Text="{Binding FileName, Mode=OneWay}" />
                                <Run Text=" : " />
                                <Run Text="{Binding Line, Mode=OneWay}" />
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
