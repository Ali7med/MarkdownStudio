<UserControl x:Class="MarkdownStudio.Controls.CommandPalette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:conv="clr-namespace:MarkdownStudio.Converters"
             Visibility="Collapsed"
             Background="#55000000">
    <UserControl.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToCollapsed" />
    </UserControl.Resources>
    <Border Width="620" MaxHeight="440" VerticalAlignment="Top" HorizontalAlignment="Center"
            Margin="0,80,0,0"
            Background="{DynamicResource ApplicationBackgroundBrush}"
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="1" CornerRadius="10" Padding="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ui:TextBox x:Name="SearchBox" Grid.Row="0"
                        Icon="{ui:SymbolIcon Search24}"
                        PlaceholderText="اكتب للبحث…"
                        TextChanged="OnQueryChanged" PreviewKeyDown="OnSearchKeyDown" />

            <ListBox x:Name="ResultList" Grid.Row="1" Margin="0,8,0,0"
                     BorderThickness="0" Background="Transparent"
                     MaxHeight="360"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="OnItemClick"
                     SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4,3">
                            <TextBlock Text="{Binding Title}" FontSize="14" />
                            <TextBlock Text="{Binding Subtitle}" FontSize="11" Opacity="0.6"
                                       TextTrimming="CharacterEllipsis"
                                       Visibility="{Binding Subtitle,
                                         Converter={StaticResource NullToCollapsed}}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
